bin/
obj/
.vs/
*.user
*.suo

# Written by tools/: a 10 MB benchmark payload, run logs, window captures.
docs/_archive/*/flood.txt
*.log
*.png
!docs/images/*.png

# Generated at build time by the bootstrapper: the packed copy of the C# build it embeds.
BetterTerminal.Bootstrap/payload.pack

# Local assembled release output; the published release is built by CI from a tag.
BUILD/

# Written by the application into whatever folder it opens.
.beterm/

# Machine-local assistant state.
.claude/settings.local.json
