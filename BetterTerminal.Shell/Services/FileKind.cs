namespace BetterTerminal.Shell.Services
{
    /// <summary>What a file turned out to be once it was read, not what its name promised.</summary>
    public enum FileKind
    {
        Text,
        Image,
        Binary
    }
}
