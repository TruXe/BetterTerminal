<Window x:Class="BetterTerminal.Shell.Views.UpdateToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="BetterTerminal update"
        Width="360" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ShowInTaskbar="False" ShowActivated="False" Topmost="True" ResizeMode="NoResize"
        UseLayoutRounding="True"
        FontFamily="{DynamicResource Bt.Font.Ui}"
        FontSize="{DynamicResource Bt.FontSize.Body}"
        Foreground="{DynamicResource Bt.TextFillPrimaryBrush}">

    <!-- A margin around the card leaves room for its shadow; the window itself is transparent. -->
    <Border Margin="12" CornerRadius="10"
            Background="{DynamicResource Bt.ElevatedBackgroundBrush}"
            BorderBrush="{DynamicResource Bt.StrokeSubtleBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="2" Opacity="0.5" Direction="270"/>
        </Border.Effect>

        <Grid Margin="16,14,14,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"
                       Style="{StaticResource Bt.Text.Icon}" FontSize="18"
                       Foreground="{DynamicResource Bt.AccentTextBrush}" Text="&#xE777;"/>
            <TextBlock Grid.Row="0" Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center"
                       Style="{StaticResource Bt.Text.BodyStrong}" Text="BetterTerminal update"/>
            <Button Grid.Row="0" Grid.Column="2" Width="24" Height="24"
                    Style="{StaticResource Bt.Button.Icon}" Click="OnClose"
                    AutomationProperties.Name="Dismiss">
                <TextBlock Style="{StaticResource Bt.Text.Icon}" FontSize="10" Text="&#xE711;"/>
            </Button>

            <TextBlock x:Name="Message" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                       Margin="10,8,0,0" TextWrapping="Wrap"
                       Foreground="{DynamicResource Bt.TextFillSecondaryBrush}"
                       Style="{StaticResource Bt.Text.Caption}"/>

            <Button Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Margin="0,14,0,0"
                    HorizontalAlignment="Right" MinWidth="104"
                    Style="{StaticResource Bt.Button.Accent}" Click="OnRestart"
                    Content="Restart now"
                    AutomationProperties.Name="Restart now to finish updating"/>
        </Grid>
    </Border>
</Window>
